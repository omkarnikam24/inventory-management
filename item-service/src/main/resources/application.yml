server:
  port: 9999
spring:
  application:
    name: item-service
  datasource:
    url: jdbc:h2:mem:itemdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    region: default
    service-url:
      default-zone: http://localhost:8761/eureka
